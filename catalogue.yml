- name: Setup catalogue component
  hosts: all
  become: yes
  vars:
    component: catalogue
    tasks:
      - name: Load nodejs
        ansible.builtin.include_vars:
          file: nodejs.yml
          name: nodejs
